
### Flag to configure registry credentials

configure_registry_credentials: true

### Red Hat Container Registry authentication (use Red Hat ID Username/Password or service account)
redhat_registry_url: registry.redhat.io
redhat_registry_username:
redhat_registry_password:

### Custom registry credentials if you are using an authenticated registry
custom_registry_url: quay.io
custom_registry_username:
custom_registry_password:

### URL and creds for AAP2 Controller
aap2_controller_url:
aap2_controller_username:
aap2_controller_password:
aap2_organization: "Default"

### URL for Red Hat Automation Hub
automation_hub_url: https://console.redhat.com/api/automation-hub/content/published/
automation_hub_auth_url: https://sso.redhat.com/auth/realms/redhat-external/protocol/openid-connect/token
automation_hub_token:

### Hostname/IP of the server hosting builds
server_hostname:

### Credentials for the server
server_username:
server_password:
server_ssh_key:
server_ssh_key_passphrase:

### OPTIONAL - AWS Credentials to push AMIs
configure_aws_credentials: false
aws_access_key:
aws_secret_key:
aws_sts_token:

### GIT Credentials configuration
configure_git_credentials: false
git_username:
git_password:
git_ssh_key:
git_ssh_key_passphrase: